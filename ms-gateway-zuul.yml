server:
   port: 8080

zuul:
   routes:
      admin:
         path: /admin
         url: ${http.scheme:http}://localhost:9000/wallboard
      simple:
         path: /simplestuff/**
         url: ${http.scheme:http}://localhost:8060/ #TODO use discovery - should be no need for port

ie:
   jor:
      example:
         configuration:
            from: zuul-configServer-remote-repo 
            