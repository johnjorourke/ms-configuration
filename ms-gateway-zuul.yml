server:
   port: 8080

zuul:
   routes:
      admin:
         path: /admin
         url: ${jor.httpSchema:http}://localhost:9000/
      simple:
         path: /simplestuff/**
         url: ${jor.httpSchema:http}://localhost:8060/

ie:
   jor:
      example:
         configuration:
            from: zuul-configServer-remote-repo 
            