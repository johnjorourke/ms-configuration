server:
   port: 8080

eureka:
  instance:
    hostname: ${MS_GATEWAY_HOST:localhost}   

zuul:
   routes:
      admin:
         path: /admin
         url: ${http.scheme:http}://${MS_ADMIN_HOST:localhost}:${MS_ADMIN_PORT:9000}/wallboard
      simple:
         path: /simplestuff/**
         url: ${http.scheme:http}://${MS_SIMPLE_HOST:localhost}:${MS_SIMPLE_PORT:8060}/ #TODO use discovery - should be no need for port

ie:
   jor:
      example:
         configuration:
            from: zuul-configServer-remote-repo 
            