server:
   port: ${MS_PORT:8080}

eureka:
   client:
      fetchRegistry: true   

zuul:
   routes:
      echo:
         path: /echo/**
         serviceId: MS-ECHO-SERVICE
      simple:
         path: /simplestuff/**
         serviceId: MS-SIMPLE-SERVICE

ie:
   jor:
      example:
         configuration:
            from: remote:ms-gateway-zuul.yml 
            