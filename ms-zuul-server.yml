server:
   port: ${MS_PORT:8080}

eureka:
   client:
      fetchRegistry: true   

zuul:
   routes:
      echox:
         path: /echo/**
         serviceId: MS-ECHO-SERVICE
      simple:
         path: /simplestuff/**
         serviceId: MS-ECHO-SERVICE

ie:
   jor:
      example:
         configuration:
            from: remote:ms-gateway-zuul.yml 
            